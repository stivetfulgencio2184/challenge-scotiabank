spring:
  r2dbc:
    url: r2dbc:h2:mem:///studentDB;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql
      #data-locations: classpath:data.sql

api:
  version: /api/v1

resource: /students