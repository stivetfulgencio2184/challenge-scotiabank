package org.alpha.omega.student_microservice.infrastructure.adapter.out.persistence.mapper.groupkey;

public record RoleGroupKey(
        Integer id,
        String name,
        String abbreviation
) {
}
